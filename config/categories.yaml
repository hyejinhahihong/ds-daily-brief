# 8개 카테고리 쿼터 — SPEC §2.1 그대로.
# tier: 1=T1(매일 2 고정) 2=T2(1~3 가변) 3=T3(0~2, 없으면 비움)
# 후보 없으면 하한을 억지로 채우지 않는다 (SPEC 원칙 3).

total_min: 12
total_max: 16   # 상한 초과 시 final_score 하위 컷 (SPEC §2.1)

categories:
  - { id: ai-agent,               name: "AI Agent",                 tier: 1, min: 2, max: 2 }
  - { id: llm-foundation-model,   name: "LLM / Foundation Model",   tier: 1, min: 2, max: 2 }
  - { id: deep-learning,          name: "Deep Learning",            tier: 2, min: 1, max: 3 }
  - { id: predictive-modeling,    name: "예측 모델링",              tier: 2, min: 1, max: 3 }
  - { id: causal-inference,       name: "인과추론",                 tier: 2, min: 1, max: 3 }
  - { id: mlops,                  name: "MLOps / ML Engineering",   tier: 1, min: 2, max: 2 }
  - { id: practice,               name: "실무 사례 · 노하우",        tier: 2, min: 1, max: 3 }
  - { id: industry-application,   name: "산업 적용 사례",           tier: 1, min: 2, max: 2 }

# 서브태그 (SPEC §2.2) — Haiku 가 해당 카테고리일 때만 부여
subtags:
  predictive-modeling: [tabular, XAI, anomaly, timeseries, imbalanced, clustering, feature-selection]
  causal-inference:    [did, psm-ipw, uplift, double-ml, causal-forest, causal-discovery, causal-llm]
